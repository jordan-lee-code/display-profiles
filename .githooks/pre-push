#!/bin/bash
# Releases are managed by .github/workflows/release.yml.
# Add a release:patch / release:minor / release:major label to your PR
# before merging — the workflow tags and publishes the release automatically.
